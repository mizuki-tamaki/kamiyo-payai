# Skip optional dependencies to avoid platform-specific issues
# (prevents @next/swc-darwin-x64 from breaking Linux builds)
omit=optional

# Use legacy peer deps resolution
legacy-peer-deps=true

# Don't save package-lock.json (it causes platform issues)
package-lock=false
